'use client'

import React from 'react'

const InfiniteScrollProducts = ({ filters }: { filters: any }) => {
  return (
    <div>
      <h2>Product List</h2>
      <p>No products to display.</p>
    </div>
  )
}

export default InfiniteScrollProducts